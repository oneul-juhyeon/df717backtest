

# 확장 가능한 다중 전략 네비게이션 시스템 설계

## 현재 상황 분석

현재 `StrategyNav`는 탭 형태로 전략을 나열합니다. 2-3개 전략에는 적합하지만, 10개 이상이 되면 다음과 같은 문제가 발생합니다:

- 화면 공간 부족 (특히 모바일)
- 스크롤 필요성 증가
- 사용자 탐색 효율성 저하

---

## 네비게이션 패턴 옵션

### 옵션 1: 드롭다운 선택기 (권장)

```text
┌─────────────────────────────────────────────────┐
│  [DF717 Logo]     [DFcovenant ▼]     [Menu]     │
│                         │                        │
│                    ┌────┴────────────┐          │
│                    │ DFcovenant  ✓   │          │
│                    │ DFtrust         │          │
│                    │ DFpath          │          │
│                    │ DFhope          │          │
│                    │ ...             │          │
│                    └─────────────────┘          │
└─────────────────────────────────────────────────┘
```

**장점:**
- 무제한 전략 수용 가능
- 현재 선택된 전략이 항상 표시됨
- 모바일에서도 동일한 UX
- 기존 shadcn/ui의 DropdownMenu 또는 Select 컴포넌트 활용

**단점:**
- 한 번의 클릭이 더 필요함

---

### 옵션 2: 커맨드 팔레트 (Cmd+K)

```text
┌─────────────────────────────────────────────────┐
│  [DF717 Logo]     [Search strategies... ⌘K]     │
└─────────────────────────────────────────────────┘

          ┌──────────────────────────────┐
          │  🔍 Search strategies...     │
          ├──────────────────────────────┤
          │  DFcovenant                  │
          │  DFtrust                     │
          │  DFpath                      │
          │  DFhope                      │
          └──────────────────────────────┘
```

**장점:**
- 검색 기능으로 빠른 접근
- 키보드 단축키 지원
- 전문적인 느낌

**단점:**
- 일반 사용자에게 익숙하지 않을 수 있음
- 모바일에서 키보드 단축키 불가

---

### 옵션 3: 사이드바 네비게이션

```text
┌────────────┬────────────────────────────────────┐
│ Strategies │                                    │
│ ───────────│           Main Content             │
│ DFcovenant │                                    │
│ DFtrust    │                                    │
│ DFpath     │                                    │
│ DFhope     │                                    │
│ ...        │                                    │
└────────────┴────────────────────────────────────┘
```

**장점:**
- 모든 전략이 한눈에 보임
- 빠른 전환

**단점:**
- 화면 공간 소모가 큼
- 현재 디자인과 어울리지 않음
- 모바일에서 추가 처리 필요

---

## 권장 솔루션: 드롭다운 선택기 + 컨텍스트 인식

드롭다운 방식을 채택하되, 현재 페이지 컨텍스트(랜딩/상세 리포트)를 인식하여 같은 타입의 페이지로 이동합니다.

### 구현 구조

```typescript
// src/config/strategies.ts
export interface Strategy {
  id: string;
  name: string;
  description: string;
  landingPath: string;
  reportPath: string;
  symbol: string;
  period: string;
}

export const strategies: Strategy[] = [
  {
    id: "dfcovenant",
    name: "DFcovenant",
    description: "Professional Gold Trading EA",
    landingPath: "/",
    reportPath: "/backtest-report",
    symbol: "XAUUSD",
    period: "H1",
  },
  {
    id: "dftrust",
    name: "DFtrust",
    description: "Conservative Gold Strategy",
    landingPath: "/dftrust",
    reportPath: "/dftrust/backtest-report",
    symbol: "XAUUSD",
    period: "H1",
  },
  // 향후 추가 전략들...
];
```

### 새로운 StrategySelector 컴포넌트

```typescript
interface StrategySelectorProps {
  currentStrategy: string;
  context: "landing" | "report";
}
```

사용 예시:
- 랜딩 페이지 헤더: `<StrategySelector currentStrategy="dfcovenant" context="landing" />`
- 상세 리포트 페이지: `<StrategySelector currentStrategy="dfcovenant" context="report" />`

### UI 디자인

```text
┌──────────────────────────────────┐
│  DFcovenant              ▼      │  <- 현재 전략 표시
└──────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────┐
│  ✓ DFcovenant                    │  <- 현재 선택됨
│    XAUUSD • H1                   │
├──────────────────────────────────┤
│    DFtrust                       │
│    XAUUSD • H1                   │
├──────────────────────────────────┤
│    DFpath                        │
│    XAUUSD • M15                  │
└──────────────────────────────────┘
```

---

## 파일 변경 계획

| 파일 | 작업 |
|------|------|
| `src/config/strategies.ts` | 새로 생성 - 전략 데이터 중앙 관리 |
| `src/components/backtest/StrategySelector.tsx` | 새로 생성 - 드롭다운 기반 전략 선택기 |
| `src/components/backtest/StrategyNav.tsx` | 삭제 또는 StrategySelector로 대체 |
| `src/components/backtest/Header.tsx` | StrategySelector 통합 |
| `src/pages/BacktestReport.tsx` | StrategySelector 사용 (context="report") |
| `src/pages/DFtrustReport.tsx` | StrategySelector 사용 (context="report") |

---

## 확장성 고려사항

### 새 전략 추가 시 작업 흐름

1. `src/config/strategies.ts`에 전략 정보 추가
2. 전략별 데이터 파일 생성 (`src/data/[strategy]Data.ts`)
3. 전략별 데이터 훅 생성 (`src/hooks/use[Strategy]Data.ts`)
4. 랜딩 페이지 생성 (`src/pages/[Strategy]Index.tsx`)
5. 상세 리포트 페이지 생성 (`src/pages/[Strategy]Report.tsx`)
6. `App.tsx`에 라우트 추가

네비게이션은 `strategies.ts`에 항목만 추가하면 자동으로 반영됩니다.

### 향후 개선 가능성

- 전략 카테고리 분류 (예: Gold, Forex, Crypto)
- 즐겨찾기 기능
- 최근 본 전략 표시
- 검색 필터 추가 (Command 컴포넌트 활용)

---

## 기술적 세부사항

### 사용할 컴포넌트

프로젝트에 이미 설치된 shadcn/ui 컴포넌트들을 활용합니다:

- `DropdownMenu` - 드롭다운 메뉴 구현
- `Command` - 향후 검색 기능 추가 시 활용 가능
- `Select` - 대안적 선택 UI

### 컨텍스트 감지 로직

```typescript
const getTargetPath = (strategy: Strategy, context: "landing" | "report") => {
  return context === "landing" ? strategy.landingPath : strategy.reportPath;
};
```

---

## 요약

| 항목 | 내용 |
|------|------|
| 선택된 패턴 | 드롭다운 선택기 |
| 최대 지원 전략 수 | 무제한 |
| 모바일 지원 | 완전 지원 |
| 컨텍스트 인식 | 랜딩/상세 리포트 간 자동 전환 |
| 새 전략 추가 난이도 | 낮음 (config 파일에 1개 항목 추가) |

